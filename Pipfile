pp[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
click = ">=8.1.0"
tabulate = ">=0.9.0"

[dev-packages]
pytest = ">=7.0.0"
pytest-mock = ">=3.0.0"

[requires]
python_version = "3.14"